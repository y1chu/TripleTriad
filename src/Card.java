public class Card {
    // The values on each side of the card
    int top, bottom, left, right;

    public Card(int top, int bottom, int left, int right) {
        this.top = top;
        this.bottom = bottom;
        this.left = left;
        this.right = right;
    }
}
